(()=>{let mode="register";const form=document.getElementById("authForm"),status=document.getElementById("status"),nameLabel=document.getElementById("nameLabel"),submit=form.querySelector("button[type=submit]");const setMode=next=>{mode=next;nameLabel.hidden=mode==="login";document.getElementById("name").required=mode==="register";document.getElementById("title").textContent=mode==="register"?"Create your account":"Welcome back";submit.textContent=mode==="register"?"Create free account":"Sign in";document.getElementById("registerTab").classList.toggle("active",mode==="register");document.getElementById("loginTab").classList.toggle("active",mode==="login")};document.getElementById("registerTab").onclick=()=>setMode("register");document.getElementById("loginTab").onclick=()=>setMode("login");if(new URLSearchParams(location.search).get("plan")==="club")document.getElementById("planNote").hidden=false;form.addEventListener("submit",async event=>{event.preventDefault();submit.disabled=true;status.textContent=mode==="register"?"Creating your account…":"Signing in…";const response=await fetch(`/api/auth/${mode}`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name:document.getElementById("name").value,email:document.getElementById("email").value,password:document.getElementById("password").value})}),result=await response.json();if(response.ok)location.href="/club";else{status.textContent=result.error;submit.disabled=false}})})();
