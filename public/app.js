const esc=s=>String(s||"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));const money=(v,c="USD")=>v==null?"See current price":new Intl.NumberFormat("en-US",{style:"currency",currency:c}).format(v);fetch("/api/products").then(r=>r.json()).then(ps=>{if(ps[0])updated.textContent="Updated "+new Date(ps[0].updated_at).toLocaleString();products.innerHTML=ps.map((p,i)=>`<article class="card"><img src="${esc(p.image_url)}"><div><span class="rank">#${i+1}</span>${p.badge?`<span class="badge">${esc(p.badge)}</span>`:""}<p class="cat">${esc(p.category)}</p><h3>${esc(p.title)}</h3><p>${esc(p.description)}</p><p class="stats">★ ${p.rating} · ${Number(p.review_count||0).toLocaleString()} reviews · Score ${p.score}</p><span class="price">${money(p.current_price,p.currency)}</span>${p.original_price?`<span class="old">${money(p.original_price,p.currency)}</span>`:""}<a class="button" href="/go/${p.id}" rel="nofollow sponsored">View current offer</a></div></article>`).join("")});