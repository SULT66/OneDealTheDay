const Database=require("better-sqlite3"),path=require("path"),fs=require("fs");const dir=path.join(__dirname,"..","data");fs.mkdirSync(dir,{recursive:true});const db=new Database(path.join(dir,"site.db"));db.pragma("journal_mode = WAL");db.exec(`CREATE TABLE IF NOT EXISTS products(id INTEGER PRIMARY KEY AUTOINCREMENT,external_id TEXT UNIQUE,title TEXT,category TEXT,description TEXT,image_url TEXT,affiliate_url TEXT,rating REAL,review_count INTEGER,current_price REAL,original_price REAL,currency TEXT,badge TEXT,score REAL,source TEXT,status TEXT,updated_at TEXT);CREATE TABLE IF NOT EXISTS refresh_runs(id INTEGER PRIMARY KEY AUTOINCREMENT,provider TEXT,started_at TEXT,finished_at TEXT,found_count INTEGER,published_count INTEGER,status TEXT,message TEXT);CREATE TABLE IF NOT EXISTS clicks(id INTEGER PRIMARY KEY AUTOINCREMENT,product_id INTEGER,clicked_at TEXT,referrer TEXT,user_agent TEXT);`);module.exports=db;